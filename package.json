{
  "name": "github-actions-practice",
  "version": "1.0.0",
  "description": "Lab 2",
  "scripts": {
    "test": "jest",
    "lint": "echo 'no lint errors'",
    "build": "mkdir -p dist && echo 'fake build artifact' > dist/bundle.js"
  },
  "devDependencies": {
    "eslint": "^9.35.0",
    "globals": "^16.4.0",
    "jest": "^29.7.0",
    "typescript-eslint": "^8.44.0"
  }
}

